extends ../../templates/template-plate.pug
include ../../mixins/plato
append config
    // TODO: * modificar variable objeto
    each plato in combos.content
        // TODO: * modificar variable TITULO
        if(plato.text == "Combo 4")
            - page = plato.text
            - metadescripcion = plato.description
block content
    .content__plate
        // TODO: * modificar variable objeto
        each plato in combos.content
            // TODO: * modificar variable TITULO
            if(plato.text == "Combo 4") 
                //- TODO: * Modificar nombre de objeto para description
                +plato(plato.img,plato.text,plato.price_uno,plato.price_dos,plato.description,sponsor[0],combos.category)
            
    include ../../blocks/btn_atras
    //- TODO: * Modificar nombre de objeto para category
    +btn(combos.category)